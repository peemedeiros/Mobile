<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item android:title="Entrada"
            android:id="@+id/menu_entrada"
            android:icon="@drawable/ic_entrada"/>

        <item android:title="Favoritos"
            android:id="@+id/menu_favoritos"
            android:icon="@drawable/ic_fav"/>

        <item android:title="Enviados"
            android:id="@+id/menu_enviados"
            android:icon="@drawable/ic_enviados"/>
    </group>
    <item android:title="Outros"
        android:icon="@drawable/ic_outros">
        <menu>
            <item android:title="Lixeira"
                android:id="@+id/menu_lixeira"
                android:icon="@drawable/ic_lixeira"/>

            <item android:title="Spam"
                android:id="@+id/menu_spam"
                android:icon="@drawable/ic_spam"/>
        </menu>
    </item>
</menu>